spring:
  application: { name: award-service }
  cloud: { nacos: { discovery: { server-addr: ${NACOS_ADDR:nacos:8848} } } }
server: { port: 8084 }
management: { endpoints: { web: { exposure: { include: health,info,metrics } } } }
